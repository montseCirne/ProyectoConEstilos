<p>Este es el menú del Mesero.</p>

<h2>Mesas</h2>
<table>
  <tr>
    <th>Mesa</th>
    <th>Estado</th>
    <th>Acción</th>
  </tr>
  {{#each mesas}}
    <tr>
      <td>{{this.dataValues.numero}}</td> <!-- Número de la mesa -->
      <td>{{this.dataValues.estado}}</td>  <!-- Estado de la mesa -->
      <td>
        <a href="/comanda/nueva/{{this..id}}">Tomar Orden</a> <!-- Siempre mostrar "Tomar Orden" -->
      </td>
    </tr>
  {{/each}}
</table>

<h2>Comandas</h2>
<p>Estas son las comandas que has tomado:</p>
<ul>
  {{#each comandas}}
    <li>Comanda #{{this.id}} - Estado: {{this.estado}}</li>
  {{/each}}
</ul>
